# VAE_MNIST
PyTorch implementation of a Variational Autoencoder (VAE) using the MNIST dataset. This project explores latent space learning, image reconstruction, representation learning, and generative deep learning concepts including encoder-decoder architecture, reparameterization trick, and VAE loss functions.
